// import type { PageLoad } from './$types';
// import { supabase } from '$lib/supabaseClient';

// export const load: PageLoad = async () => {
// 	const { data } = await supabase.from('juancito table').select('*');

// 	return { data: data };
// };
